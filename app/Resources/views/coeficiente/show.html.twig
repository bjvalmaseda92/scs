{% extends 'base.html.twig' %}

{% block body %}
    <h1>Coeficiente</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coeficiente.id }}</td>
            </tr>
            <tr>
                <th>Mes</th>
                <td>{{ coeficiente.mes }}</td>
            </tr>
            <tr>
                <th>Ano</th>
                <td>{{ coeficiente.ano }}</td>
            </tr>
            <tr>
                <th>Cuc</th>
                <td>{{ coeficiente.cuc }}</td>
            </tr>
            <tr>
                <th>Cup</th>
                <td>{{ coeficiente.cup }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('coeficiente_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('coeficiente_edit', { 'id': coeficiente.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
