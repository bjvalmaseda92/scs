{% extends 'base.html.twig' %}

{% block body %}
    <h1>Escala</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ escala.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ escala.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('escalas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('escalas_edit', { 'id': escala.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
